# Smart Budget Tracker — Backend

## Quick start
1. Ensure PostgreSQL is running (see root `docker-compose.yml`).
2. Configure `src/main/resources/application.properties` if needed.
3. Build & run:
```bash
mvn spring-boot:run
```
Open: `http://localhost:8080/swagger-ui/index.html`
